<div class="container m-5">
    <div class="row">
        <div class="card">
            <h5 class="card-header">Mi perfil</h5>
            <div class="card-body">
                <h5 class="card-title">Usuario</h5>
                    <p class="card-text">Nombre: {{user.first_name}} {{user.last_name}}</p>
                    <p class="card-text">Email: {{user.email}}</p>
                    <p class="card-text">Edad: {{user.age}}</p>
                    <p class="card-text">Rol: {{user.role}}</p>
                    <a href="/products" class="btn btn-outline-dark">Volver</a>
                    <button type="button" class="btn btn-dark" id="btnLogout">Log Out</button>
            </div>
        </div>
    </div>                      
</div>

<script src="/js/profile.js"></script>

